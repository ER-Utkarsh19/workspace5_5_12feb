file(GLOB_RECURSE BACNET_SOURCES
    "src/bacnet/*.c"
    "src/ports/esp32/*.c"
)

idf_component_register(
    SRCS ${BACNET_SOURCES}
    INCLUDE_DIRS "include" "src"
    REQUIRES lwip esp_wifi esp_netif
)
